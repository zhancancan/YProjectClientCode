using pure.lua;
using pure.ui.element;
using UnityEngine;

namespace mono.ui.elements {
    [LuaExport,AddComponentMenu("UI/Draggable", 36), RequireComponent(typeof (RectTransform))]
    public class Draggable : PDragabel_Dll {
    }
}