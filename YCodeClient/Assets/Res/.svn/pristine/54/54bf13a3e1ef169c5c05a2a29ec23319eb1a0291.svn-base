using pure.scene.mono;
using pure.utils.dlltools;
using UnityEngine;

namespace mono.scene {
    [ExecuteInEditMode, MonoDll(MonoType.CUSTOM_DATA)]
    public sealed class CustomData : CustomData_Dll {
    }
}