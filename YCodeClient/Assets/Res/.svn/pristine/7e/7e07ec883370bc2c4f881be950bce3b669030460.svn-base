using pure.lua;
using pure.ui.element;
using UnityEngine;

namespace mono.ui.elements {
    [LuaExport,AddComponentMenu("UI/Dropdown", 34), RequireComponent(typeof (RectTransform)), ExecuteInEditMode]
    public sealed class PDropdown : PDropDown_Dll {
    }
}