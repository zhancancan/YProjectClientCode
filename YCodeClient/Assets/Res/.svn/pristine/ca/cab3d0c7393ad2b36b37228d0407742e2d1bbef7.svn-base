require "UnsaveProxy"
local class = require "Class"
local base=require "UnsaveProxy"
local this = class("Proxy_LoginMasterResponse",base)
_G["Proxy_LoginMasterResponse"]=this


function this:ctor()
	self._proxycenter=require"ProxyCenter"
end
function this:pushData(buffer,_)
	local data=self:_parseData(buffer)
	if not data then return end
	print("Proxy_LoginMasterResponse",data)
end
