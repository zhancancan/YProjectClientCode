using pure.scene.mono;
using pure.utils.dlltools;
using UnityEngine;

namespace mono.scene {
    [ExecuteInEditMode, MonoDll(MonoType.EVENT_AREA)]
    public sealed class EventArea : EventArea_Dll {
    }
}