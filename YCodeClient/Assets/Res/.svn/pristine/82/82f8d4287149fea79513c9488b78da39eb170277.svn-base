using pure.lua;
using pure.ui.element;
using UnityEngine;

namespace mono.ui.elements {
    [LuaExport, AddComponentMenu("UI/Clickable", 36), RequireComponent(typeof (RectTransform))]
    public sealed class Clickable : PClickable_Dll {
    }
}