using pure.lua;
using pure.ui.element;
using UnityEngine;

namespace mono.ui.elements {
    [LuaExport, AddComponentMenu("UI/Sprite Graphic", 38), RequireComponent(typeof (RectTransform)), ExecuteInEditMode]
    public sealed class SpriteGraphic : PSpriteGraphic_Dll {
    }
}