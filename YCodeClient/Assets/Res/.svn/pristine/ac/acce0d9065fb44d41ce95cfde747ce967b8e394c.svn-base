using pure.scene.mono;
using pure.utils.dlltools;
using UnityEngine;

namespace mono.scene {
    [ExecuteInEditMode, RequireComponent(typeof (MeshCollider)), MonoDll(MonoType.NAVIGATOR)]
    public class Navigator : Navigator_Dll {
    }
}