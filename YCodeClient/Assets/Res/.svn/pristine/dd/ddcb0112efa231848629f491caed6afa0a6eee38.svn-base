using pure.lua;
using pure.ui.element;
using UnityEngine;

namespace mono.ui.elements {
    [LuaExport,AddComponentMenu("UI/TouchControl", 33), RequireComponent(typeof (RectTransform))]
    public sealed class TouchControl : PTouch_Dll {
    }
}